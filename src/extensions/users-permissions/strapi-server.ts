module.exports = (plugin) => {
    // Add a new attribute "eloRating" to the User model with a default value (e.g. 1000)
    // plugin.models.user.attributes.eloRating = {
    //   type: "integer",
    //   default: 1000
    // };
    return plugin;
  };
  